    holira
    {{!-- <div class="album py-5 bg-light">
        <div class="container">
            <div class="row">
               {{#each }}
                <div class="col-md-4">
              <div class="card mb-4 shadow-sm">
                <img class="card-img-top artist-cover" src="{{ this.images.0.url }}" alt="{{this.name}}">
                <div class="card-body">
                  <h5>{{this.name}}</5>
                  <div class="d-flex justify-content-between align-items-center">
                    <div class="btn-group pt-3">
                      <a href="/albums/{{this.id}}" class="btn btn-sm  btn-outline-secondary">View artist</a> 
                    </div>
                  </div>
                </div>
              </div>
            </div>
              {{/each}}
            </div>
        </div>
    </div> --}}